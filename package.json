{
  "name": "jep-replay-visualizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check src/replayEngine.js && node --check src/app.js"
  }
}
